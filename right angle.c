
include <stdio.h>
#include<math.h>
int main()
{
  float a,b,c,x,y;
  printf("Enter the length of three side A,B and C:\n");
  scanf("%f %f %f",&a,&b,&c);
x=pow(a,2)+pow(b,2);
y=pow(c,2);
int sl=(x==y)?(x==y):(x==y);
printf("%d",sl);
  
}
